# FinSight uses a local Ollama model — no paid API key required.
# Only override this if your Ollama server runs somewhere other than
# the default local address (e.g. a remote host, or a different port).
OLLAMA_BASE_URL=http://localhost:11434
